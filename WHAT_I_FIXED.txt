Project LOOP - minimal run fixes

1. Created BackEnd/.env with PORT=5000, local MongoDB URI, JWT secret, and the supplied Gemini API key.
2. Renamed FrontEnd/src/Pages/AskAi.jsx to AskAI.jsx so it matches the existing App.jsx import.
3. Added RUN_PROJECT.bat as an optional Windows helper. It does not change application logic or design.

To run manually in VS Code:
- Ensure MongoDB is running.
- Terminal 1: cd BackEnd && npm install && npm run dev
- Terminal 2: cd FrontEnd && npm install && npm run dev
- Open http://localhost:5173

Security note: the Gemini key was shared in chat/screenshot. Replace it with a newly generated key in BackEnd/.env before using this project beyond local testing.
